create table userEntity(
                           id int primary key auto_increment,
                           Name varchar(100),
                           latitude double,
                           longitude double,
                           distance double,
                           excluded int
);
